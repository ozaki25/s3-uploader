service: my-express-application

provider:
  name: aws
  runtime: nodejs8.10
  region: ap-northeast-1
  stage: dev

functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'
